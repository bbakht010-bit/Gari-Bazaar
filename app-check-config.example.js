/**
 * Copy to `app-check-config.js` and paste your reCAPTCHA v3 site key from:
 * Firebase Console → App Check → Apps → Web app → Register (reCAPTCHA v3).
 *
 * The site key is public (safe in Hosting). After saving, redeploy Functions so
 * callable enforcement turns on automatically when a key is detected.
 */
window.GARI_BAZAAR_APP_CHECK_SITE_KEY = "PASTE_RECAPTCHA_V3_SITE_KEY_HERE";
// Optional local/emulator debug token from Firebase Console → App Check → Manage debug tokens
// window.GARI_BAZAAR_APP_CHECK_DEBUG_TOKEN = "YOUR_DEBUG_TOKEN_UUID";
